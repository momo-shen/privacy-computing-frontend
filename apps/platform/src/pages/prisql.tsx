import {HomeLayout} from "@/modules/layout/home-layout";
import {PrisqlLayout} from "@/modules/layout/prisql-layout";

const PsiPage = () => {
  return (
      <HomeLayout>
        <PrisqlLayout/>
      </HomeLayout>
  );
};

export default PsiPage;
